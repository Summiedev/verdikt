spring:
  application:
    name: verdikt-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/verdikt?useTimezone=true&serverTimezone=UTC
    username: postgres
    password: summie
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          time_zone: UTC


app:
  allowed-origins: http://localhost:5173,http://localhost:3000